- name: Install MariaDB server
  apt:
    name: mariadb-server
    state: present
    update_cache: true
    cache_valid_time: 3600

- name: Ensure MariaDB is started and enabled
  service:
    name: mariadb
    state: started
    enabled: true

